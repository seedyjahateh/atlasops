node_modules/
dist/
coverage/
pnpm-lock.yaml

# Generated from tools/boundaries/layers.json. Prettier and the generator would
# disagree about table alignment, and the generator is the source of truth.
docs/MODULES.md

# Generated by `pnpm corpus:inventory`. Prettier collapses a single-element array
# onto one line and the generator writes it expanded, so with both running, the
# inventory check would fail immediately after the inventory was regenerated —
# and the fix somebody would reach for is to stop running the check.
examples/corpus.inventory.json

# Mirrored from the portfolio repository; formatting belongs to the canonical copy.
docs/prd/

# Parser fixtures. Their exact bytes are the input under test — character offsets,
# blank lines and table alignment are the thing being asserted, so a formatter that
# improved them would silently rewrite the test.
packages/ingest/fixtures/
