# Promotion readiness

**Verdict: not ready. The manifest stays at `proofLevel: "code"`.**

PRD 12 lists seven required artefacts and states the rule plainly: "Until every item is present, the
manifest stays at `code`, and no number from section 4.5, 8, or 9.3 may be restated anywhere as an
achievement." Five of the seven exist. Two cannot be produced by this repository as it stands, and
neither is close.

This document is the item-by-item assessment. It is deliberately the last thing P12 produces,
because the honest output of an evidence phase is sometimes a refusal.

---

## The seven items

| #   | Artefact                        | Status                                      |
| --- | ------------------------------- | ------------------------------------------- |
| 1   | A running system                | **Met**                                     |
| 2   | A published evaluation report   | **Produced, and supports no quality claim** |
| 3   | A published governance report   | **Met**                                     |
| 4   | A cost and latency report       | **Not met**                                 |
| 5   | A boundary-enforcement artefact | **Not met**                                 |
| 6   | A threat model                  | **Met**                                     |
| 7   | An honest limitations list      | **Met**                                     |

---

### 1. A running system — met

Four components, each runnable from a documented command, against a named corpus snapshot with a
recorded hash.

```bash
pnpm app:ingest     # crawl examples/corpus and report
pnpm app:api        # answer API on http://127.0.0.1:8080
pnpm app:eval       # run every ablation arm, write evidence/
pnpm app:console    # read-only console on http://127.0.0.1:8081
```

All four were run end to end during P11b rather than assumed: the worker crawled the example corpus
and wrote six chunks, the runner ran four arms and wrote five artefacts, the API served a cited
answer over HTTP, and the console served the artefact index and refused a path traversal. The
runner records the corpus snapshot hash on every artefact.

What "running" means here is bounded by the limitations: the system runs against stand-ins, not
against models.

### 2. A published evaluation report — produced, and supports no quality claim

`pnpm app:eval` generates one report per ablation arm plus the ablation deltas, recording dataset
versions and hashes, the corpus snapshot hash, model identifiers by role, the judge's pinned model
and prompt version, run count, the per-query records PRD 8.5 requires, and the full PRD 8.2 metric
table with every row that could be computed and a stated reason for every row that could not.

**It nonetheless supports no claim about retrieval or answer quality**, because every model in it is
a stand-in with no semantic structure. The numbers are real measurements of a real run of a system
that does not do the thing being measured. PRD 12 item 2 asks for the report and this is the report;
what it cannot do is carry `proofLevel: measured`, and nothing in it may be restated as an
achievement.

The artefacts also record, on their face, that the in-repo datasets are labelled against no corpus
and that the run therefore used `--allow-snapshot-mismatch`. That is a second reason the numbers are
not comparable to anything.

### 3. A published governance report — met

`evidence/governance.md`, generated by the same run. It records the probe set version and hash, the
number of probes, the leak count, the prompt-injection subset result, the existence-disclosure
count, and the audit-record schema derived from a record the run actually wrote.

**This is the one required artefact this build produces without qualification.** A leak count
measures the permission pre-filter, which is ordinary code with no model in it, so the number is
about the system that ships rather than about a stand-in. The leak count is zero; PRD 8.4 makes a
non-zero count a build failure rather than a caveat, so a run that leaked would not have produced
the file at all.

### 4. A cost and latency report — not met

PRD 12 item 4 requires "a scripted load run against the reference profile of 9.1, with the full
stage breakdown, the concurrency level, the hardware description, the versioned price table used,
and the raw span export."

**The report now exists** — `pnpm loadrun` writes `evidence/cost-and-latency.md` from the committed
record in `docs/measurements/load-run.json`, over a stated workload, at a stated concurrency, on
described hardware, with the stage breakdown and the sample size behind every figure. That is most
of the artefact, and the half that was missing when P12 wrote this section.

It is still **not met**, for a reason no amount of harness fixes:

- **No model in the run is real.** The embedder, reranker and generator are the in-repo stand-ins,
  so no request leaves the machine and the generation stage is a local function call. The latencies
  for permission, retrieval and verification are the code that ships; the end-to-end figure omits
  the largest component a deployed system has. It is a floor, not an estimate, and PRD 12 item 4
  asks for a run against the reference profile — which pins model identifiers precisely so that a
  run against stand-ins cannot be read as one against models.
- **Cost stays unmeasurable.** A price table for the OpenAI models exists since P13, but nothing in
  this run used them, and a stand-in has no price (ADR 0002). Four of the ten budgets therefore
  report themselves unmeasured rather than zero: returning zero would make every cost budget in
  PRD 9.3 pass trivially while the real figure surfaced on an invoice.
- **Time to first token is unmeasurable too**, for a third reason: the adapter speaks the
  non-streaming endpoint (ADR 0006), and a first-token time cannot be inferred from a whole-response
  latency.

Five of the ten budgets are measured and within target. Naming them here would be the exact mistake
PRD section 0 forbids, because "within target" on this run means "within target when no model is
called". The artefact says so on its own face, and so does this line.

**What CI enforces, and the gap that leaves.** PRD 9.3 wants budgets enforced in CI against the
reference profile. A load run cannot go in CI — against real models it costs money on every push,
and against stand-ins it would measure the runner. So `pnpm loadrun:check` enforces the budgets
against the committed record and fails the build on a breach, which was demonstrated rather than
asserted. A regression therefore surfaces at the next deliberate run rather than at the next push,
and between runs the enforcement is only as current as the record's date — which the check prints.

### 5. A boundary-enforcement artefact — met

**Met as of P17b, and decided by the generator rather than by this document.** `pnpm
boundaries:evidence` now reads each exhibit's outgoing edges and reports that
`exhibits/rag-02-codebase` and `exhibits/rag-03-incident` each consume declared packages and neither
imports another exhibit or an application. Demonstrated: RAG-03 importing RAG-02 — by package name and
by relative path — produced two `exhibit-is-a-leaf` violations, and removing the imports returned
exit 0. The generator previously said "not met" unconditionally; a verdict that cannot change is not
evidence, so it now computes one, with tests for each way it can fail.

The history below is kept because the correction in it matters.

`pnpm boundaries:evidence` writes `evidence/boundaries.md`: the check result over every declared
module, and the module dependency graph as a sorted edge list. Both halves are real.

The third clause is not met. PRD 12 item 5 requires the graph to demonstrate **at least two exhibits
consuming `packages/*` and importing no other exhibit**, and this repository contains **one**:
`exhibits/rag-02-codebase`, added in P16. The second is P17.

**A correction to what this section said before P16.** It stated that `exhibit-is-a-leaf` "would fail
CI today". That was true only for an import written as a relative path. The first exhibit showed the
normal form — importing a workspace package by its name, `@atlasops/exhibit-rag-02-codebase` — passed
the checker as though it were an npm dependency, because the checker knew only the names of declared
packages. It now reads every application's and exhibit's package name, and a package importing an
exhibit by name was demonstrated failing. The rule had been tested against the one form nobody
would use, which is the argument PRD 11 makes for needing real exhibits: an enforced rule with
nothing to enforce it against is a weaker claim than it looks, and this one was weaker than stated.

The generator says so in the file it writes rather than rendering the half it can and letting the
absence pass unremarked.

### 6. A threat model — met

`docs/threat-model.md`. Tenant isolation, prompt injection through retrieved content, cache-key
leakage and existence disclosure, each with the PRD section 6 mitigation and the named test that
exercises it, plus the residual risk in each case. Mitigations without a named test are listed as
unmitigated.

### 7. An honest limitations list — met

`docs/limitations.md`. The four PRD 12 names, four more this build actually has — chiefly that no
run has used a real model and what follows from that — and the scope boundaries that are choices
rather than gaps.

---

## What promotion would require

In rough order of effort:

1. ~~**A provider adapter behind `model-gateway`'s interfaces**, with a real price table.~~ **Built
   in P13** (ADR 0006): an OpenAI embedding and generation adapter, and a dated price table for the
   models it uses. It unblocks item 4's cost half **only once a run actually uses it** — no
   application constructs it yet, and no artefact here was produced with it, so nothing in this
   document has changed status because of it. There is still no reranker adapter.
2. ~~**A load harness** producing a stage breakdown at a stated concurrency on described hardware.~~
   **Built in P15b.** `pnpm loadrun` produces it and `pnpm loadrun:check` enforces the budgets in
   CI against the committed record. Item 4 stays unmet because the run used no real model, not
   because the harness is missing: **the same command against a configured provider is what closes
   it.**
3. ~~**Two exhibits** consuming `packages/*` and importing no other exhibit.~~ **Built in P16 and
   P17b**, with the shared construction promoted into `packages/sandbox` in P17a. Item 5 is met.
4. **A re-run of the evaluation** against a corpus the datasets are actually labelled for, so the
   snapshot matches and `--allow-snapshot-mismatch` is not needed.
5. **Selection of the unselected numbers** — `k`, the retriever depths, the support threshold, the
   regression tolerance — on the labelled development split, recorded in the artefact, with their
   provenance flipped from `unselected-default`.

Only then do the manifest changes PRD 12 lists become available, and they are a reviewed edit to
`content/projects/RAG-01.json` in the portfolio repository — **not** something this repository or
this build performs. That separation is the point: a project that could promote itself would be a
project whose proof level means nothing.

---

## Where the artefacts are

Generated, not committed — `evidence/` is gitignored because these are reproducible outputs rather
than working files:

```bash
pnpm evidence       # boundaries.md, then the evaluation and governance artefacts
```

Written and committed, because they are arguments rather than measurements:

- `docs/threat-model.md`
- `docs/limitations.md`
- `docs/promotion-readiness.md` (this file)
