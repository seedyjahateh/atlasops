<!-- Generated by `pnpm readiness` from the published artefacts. Do not edit by hand: `pnpm readiness:check` fails when this file disagrees with them. -->

# Promotion readiness

**Verdict: all seven PRD 12 items are met. A manifest edit is proposed for review; nothing has been promoted, and the manifest stays at `proofLevel: "code"` until a person makes that edit.**

PRD 12 lists seven artefacts that must exist before the manifest may move from `code` to
`measured`. Each verdict below is computed by `tools/readiness` from the file that is supposed to
satisfy the item — fields present, counts consistent across files, references that resolve — and
every finding it made is listed, including the ones that hold. Until P18c this document was
written by hand, and it went stale the way hand-written verdicts do.

A met item is a statement about what an artefact contains. It is not a statement that the
numbers in it are good; several are not, and the limitations list and the notes for the reviewer
say which.

## The seven items

| # | Artefact | Verdict |
| --- | --- | --- |
| 1 | A running system | **Met** |
| 2 | A published evaluation report | **Met** |
| 3 | A published governance report | **Met** |
| 4 | A published cost and latency report | **Met** |
| 5 | A boundary-enforcement artefact | **Met** |
| 6 | A threat model | **Met** |
| 7 | An honest limitations list | **Met** |

### 1. A running system — met

- Holds: `pnpm app:ingest` runs `apps/ingest-worker/src/main.ts`, which exists.
- Holds: `pnpm app:ingest` is documented in `apps/README.md`.
- Holds: `pnpm app:api` runs `apps/api/src/main.ts`, which exists.
- Holds: `pnpm app:api` is documented in `apps/README.md`.
- Holds: `pnpm app:eval` runs `apps/eval-runner/src/main.ts`, which exists.
- Holds: `pnpm app:eval` is documented in `apps/README.md`.
- Holds: the named corpus snapshot is `sha256:f05a2049d6de1bb249071782efe6e5d32e092a7fb39bf65e66af376b5179b8b9`, recorded in `examples/corpus.inventory.json`.
- Holds: the `dense-only` evaluation ran against the named snapshot, and its datasets are labelled for it.
- Holds: the `lexical-only` evaluation ran against the named snapshot, and its datasets are labelled for it.
- Holds: the `fused-no-rerank` evaluation ran against the named snapshot, and its datasets are labelled for it.
- Holds: the `fused-with-rerank` evaluation ran against the named snapshot, and its datasets are labelled for it.
- Holds: the load run served the named snapshot.

Decided from `package.json`, `apps/README.md`, `examples/corpus.inventory.json`, `docs/evidence/run-dense-only.json`, `docs/evidence/run-lexical-only.json`, `docs/evidence/run-fused-no-rerank.json`, `docs/evidence/run-fused-with-rerank.json`, `docs/measurements/load-run.json`.

### 2. A published evaluation report — met

- Holds: the `dense-only` arm has a run record.
- Holds: `docs/evidence/run-dense-only.json` is the record of the `dense-only` arm.
- Holds: the `dense-only` arm has a rendered report.
- Holds: the `dense-only` record carries every field item 2 names: dataset versions and hashes, corpus snapshot hash, commit, the four model identifiers, prompt versions, run count, seeds.
- Holds: the `dense-only` arm's embedder and generator are models, not stand-ins.
- Holds: the `dense-only` arm's raw per-query file has 29 record(s), and the report declares 29.
- Holds: the `dense-only` metric table covers every PRD 8.2 dimension: 15 row(s), each with a sample size, and 1 dimension(s) stated unavailable with a reason.
- Holds: the `lexical-only` arm has a run record.
- Holds: `docs/evidence/run-lexical-only.json` is the record of the `lexical-only` arm.
- Holds: the `lexical-only` arm has a rendered report.
- Holds: the `lexical-only` record carries every field item 2 names: dataset versions and hashes, corpus snapshot hash, commit, the four model identifiers, prompt versions, run count, seeds.
- Holds: the `lexical-only` arm's embedder and generator are models, not stand-ins.
- Holds: the `lexical-only` arm's raw per-query file has 29 record(s), and the report declares 29.
- Holds: the `lexical-only` metric table covers every PRD 8.2 dimension: 15 row(s), each with a sample size, and 1 dimension(s) stated unavailable with a reason.
- Holds: the `fused-no-rerank` arm has a run record.
- Holds: `docs/evidence/run-fused-no-rerank.json` is the record of the `fused-no-rerank` arm.
- Holds: the `fused-no-rerank` arm has a rendered report.
- Holds: the `fused-no-rerank` record carries every field item 2 names: dataset versions and hashes, corpus snapshot hash, commit, the four model identifiers, prompt versions, run count, seeds.
- Holds: the `fused-no-rerank` arm's embedder and generator are models, not stand-ins.
- Holds: the `fused-no-rerank` arm's raw per-query file has 29 record(s), and the report declares 29.
- Holds: the `fused-no-rerank` metric table covers every PRD 8.2 dimension: 16 row(s), each with a sample size, and 1 dimension(s) stated unavailable with a reason.
- Holds: the `fused-with-rerank` arm has a run record.
- Holds: `docs/evidence/run-fused-with-rerank.json` is the record of the `fused-with-rerank` arm.
- Holds: the `fused-with-rerank` arm has a rendered report.
- Holds: the `fused-with-rerank` record carries every field item 2 names: dataset versions and hashes, corpus snapshot hash, commit, the four model identifiers, prompt versions, run count, seeds.
- Holds: the `fused-with-rerank` arm's embedder and generator are models, not stand-ins.
- Holds: the `fused-with-rerank` arm's raw per-query file has 29 record(s), and the report declares 29.
- Holds: the `fused-with-rerank` metric table covers every PRD 8.2 dimension: 16 row(s), each with a sample size, and 1 dimension(s) stated unavailable with a reason.
- Holds: all four arms ran at the same commit, over the same corpus snapshot, the same dataset versions and the same splits — so their deltas are comparable.
- Holds: the records mark `fused-no-rerank` as the served configuration.
- Holds: the ablation report names every arm.

Decided from `docs/evidence/run-dense-only.json`, `docs/evidence/run-dense-only.md`, `docs/evidence/run-dense-only.queries.jsonl`, `docs/evidence/run-lexical-only.json`, `docs/evidence/run-lexical-only.md`, `docs/evidence/run-lexical-only.queries.jsonl`, `docs/evidence/run-fused-no-rerank.json`, `docs/evidence/run-fused-no-rerank.md`, `docs/evidence/run-fused-no-rerank.queries.jsonl`, `docs/evidence/run-fused-with-rerank.json`, `docs/evidence/run-fused-with-rerank.md`, `docs/evidence/run-fused-with-rerank.queries.jsonl`, `docs/evidence/ablation.md`.

### 3. A published governance report — met

- Holds: the governance report exists.
- Holds: the probe set is `corpus-permission-probe@1.0.0`, recorded with its hash.
- Holds: the report records 6 probe(s) executed.
- Holds: the leak count is 0.
- Holds: the prompt-injection subset result is recorded: 0 leak(s) over 2 probe(s).
- Holds: the prompt-injection subset leaked 0 time(s).
- Holds: the audit-record schema is recorded: 14 field(s), derived from a record the run wrote.
- Holds: every arm's own metric table also records a leak count of 0.
- Holds: the governance report comes from the same run as the served arm's evaluation.

Decided from `docs/evidence/governance.md`, `docs/evidence/run-dense-only.json`, `docs/evidence/run-lexical-only.json`, `docs/evidence/run-fused-no-rerank.json`, `docs/evidence/run-fused-with-rerank.json`.

### 4. A published cost and latency report — met

- Holds: the load-run record exists and parses.
- Holds: the run records its reference profile `local-openai-c4`: corpus snapshot, fixed workload, model identifiers, hardware (12 x 12th Gen Intel(R) Core(TM) i5-1245U, 16 GB, win32/x64, single process) and concurrency 4.
- Holds: the embedder and generator are models (`text-embedding-3-small`, `gpt-4.1-mini`), so the run measured the system rather than a stand-in.
- Holds: costs were computed from the versioned price table `openai-2026-09-24`, which is checked in.
- Holds: the stage breakdown has the 10 PRD 9.2 stages the served configuration runs, and the end-to-end figure, each with its sample count; reranking is recorded as bypassed rather than measured.
- Holds: the raw span export has 260 request trace(s), and the record declares 260 request(s).
- Holds: 9 of 10 PRD 9.3 budgets are measured, and each unmeasured one states why.
- Holds: the rendered report is published and cites the run's commit `cd06e78`.

Decided from `docs/measurements/load-run.json`, `docs/measurements/load-run.spans.jsonl`, `docs/evidence/cost-and-latency.md`, `packages/model-gateway/src/prices-openai.ts`.

### 5. A boundary-enforcement artefact — met

- Holds: the boundary artefact exists.
- Holds: it records `pnpm boundaries:check` passing.
- Holds: the generated dependency graph has 88 edge(s).
- Holds: 2 exhibit(s) in the graph consume `packages/*` and import no other exhibit or application: `exhibits/rag-02-codebase`, `exhibits/rag-03-incident` — item 5 requires at least two.
- Holds: the artefact's own verdict is "Met", and agrees with the edges only if that is "Met".

Decided from `docs/evidence/boundaries.md`.

### 6. A threat model — met

- Holds: the threat model exists.
- Holds: "Tenant isolation" has a section with mapped mitigations.
- Holds: "Tenant isolation": 8 mitigation(s), each mapped to a test file that exists; 8 name the test case, which was found in that file, and 0 name the file and describe the case.
- Holds: "Prompt injection through retrieved content" has a section with mapped mitigations.
- Holds: "Prompt injection through retrieved content": 5 mitigation(s), each mapped to a test file that exists; 3 name the test case, which was found in that file, and 2 name the file and describe the case.
- Holds: "Cache-key leakage" has a section with mapped mitigations.
- Holds: "Cache-key leakage": 4 mitigation(s), each mapped to a test file that exists; 3 name the test case, which was found in that file, and 1 name the file and describe the case.
- Holds: "Existence disclosure" has a section with mapped mitigations.
- Holds: "Existence disclosure": 7 mitigation(s), each mapped to a test file that exists; 4 name the test case, which was found in that file, and 3 name the file and describe the case.

Decided from `docs/threat-model.md`.

### 7. An honest limitations list — met

- Holds: the limitations list exists.
- Holds: it states the synthetic corpus.
- Holds: it states the English-only v1.
- Holds: it states the absence of real user traffic.
- Holds: it states judge-model bias in groundedness metrics.

Decided from `docs/limitations.md`.

## The proposal

Because all seven items hold, `pnpm readiness` wrote `docs/promotion/RAG-01.proposed.json`: every field PRD 12
lists, with the value the artefacts support. **It is a file for review, and nothing has been
promoted.** The manifest is a reviewed edit in the portfolio repository, which this repository
never writes; until that edit is made, `proofLevel` is still `code`.

| Field | Proposed |
| --- | --- |
| `status` | `in-progress` |
| `proofLevel` | `measured` |
| `content.problem` | from PRD section 1, 463 characters |
| `content.limitations` | 13 entries from `docs/limitations.md` |
| `stack` | languages: typescript; infrastructure: github-actions; testing: vitest |
| `metrics` | 19, each with `environment`, `sampleSize`, `synthetic: true`, `measuredAt` and `evidenceUrl` |
| `evidence` | 7, primary: `evaluation-served-configuration` |
| `dates.started` | `2026-09-20` |
| `dates.lastVerified` | `2026-09-25` |
| `integrity.reviewedBy` | left for the reviewer |

Proposed metrics, each value copied unrounded from the artefact named in the proposal's
`provenance`:

| Metric | Value | Unit | Sample size |
| --- | --- | --- | --- |
| Retrieval recall@10, served configuration | 0.9242424242424242 | ratio | 11 |
| Retrieval nDCG@10, served configuration | 0.8663381881612779 | ratio | 11 |
| Retrieval mean reciprocal rank, served configuration | 0.859090909090909 | ratio | 11 |
| Citation precision | 1 | ratio | 6 |
| Citation recall | 1 | ratio | 6 |
| Cited spans that resolve to the cited source text | 1 | ratio | 6 |
| Correct abstention on questions the corpus cannot answer | 0.75 | ratio | 4 |
| Abstention on questions the corpus can answer | 0 | ratio | 2 |
| Permission leaks over the probe set | 0 | count | 6 |
| Existence disclosures over the probe set | 0 | count | 6 |
| End-to-end answer latency p95, all requests (95% retrieval-cache hits) | 4755.820900000001 | ms | 260 |
| Time to the model's first token p95, from the start of the request | 726.6957000000002 | ms | 260 |
| Retrieval stage latency p95, both retrievers and fusion | 564.0732000000007 | ms | 13 |
| Permission resolution and predicate compilation latency p95 | 0.030600000012782402 | ms | 260 |
| Citation verification latency p95 | 0.05490000000281725 | ms | 260 |
| Cost per answered query p50, excluding reranking | 0.0008052000000000001 | usd | 218 |
| Cost per answered query p95, excluding reranking | 0.002122 | usd | 218 |
| Ingestion embedding cost per 1,000 chunks | 0.0010000000000000002 | usd-per-1k | 1 |
| End-to-end answer latency p95, retrieval-cache misses only (the slowest sample) | 8211.4041 | ms | 13 |

Left out, each for a stated reason:

- contribution:dense (docs/evidence/run-fused-no-rerank.json) — a diagnostic of how fusion weighed the retrievers, not a property of the answers.
- contribution:lexical (docs/evidence/run-fused-no-rerank.json) — a diagnostic of how fusion weighed the retrievers, not a property of the answers.
- supported-claim-rate (docs/evidence/run-fused-no-rerank.json) — scored by `stand-in-judge`, a stand-in judge: PRD 8.3 does not let a judged metric gate a release alone, and a stand-in's score is not evidence of groundedness at all.
- contradiction-rate (docs/evidence/run-fused-no-rerank.json) — scored by `stand-in-judge`, a stand-in judge: PRD 8.3 does not let a judged metric gate a release alone, and a stand-in's score is not evidence of groundedness at all.
- judge-human-agreement (docs/evidence/run-fused-no-rerank.json) — scored by `stand-in-judge`, a stand-in judge: PRD 8.3 does not let a judged metric gate a release alone, and a stand-in's score is not evidence of groundedness at all.
- cost-per-answer (docs/evidence/run-fused-no-rerank.json) — counts generation only; the load run's cost per answered query is the whole-request figure and is proposed instead.
- every metric of the `dense-only` arm (docs/evidence/run-dense-only.json) — an ablation arm is evidence about the served configuration, not a description of it; it is linked through the ablation report.
- every metric of the `lexical-only` arm (docs/evidence/run-lexical-only.json) — an ablation arm is evidence about the served configuration, not a description of it; it is linked through the ablation report.
- every metric of the `fused-with-rerank` arm (docs/evidence/run-fused-with-rerank.json) — an ablation arm is evidence about the served configuration, not a description of it; it is linked through the ablation report.
- RERANK-STAGE-P95 (docs/measurements/load-run.json) — unmeasured: the served configuration bypasses reranking (ADR 0011), so no request has a rerank stage to measure.
- RETRIEVAL-ONLY-COST (docs/measurements/load-run.json) — 95% of requests were served from the retrieval cache and never embedded a query, so the figure measures the cache rather than a retrieval.

For the reviewer:

- Nothing in this file has been applied. It is a proposal for a reviewed edit to the portfolio manifest, which this repository never writes.
- Every URL points at `main`. Pin each to the commit you review, so that the evidence cannot change under the manifest after it is promoted.
- `integrity.reviewedBy` and `integrity.reviewedAt` are left null on purpose: they record a person's review, and a generator cannot perform one.
- `status` is `in-progress` rather than `complete` because the limitations list records a PRD requirement as unimplemented: "PRD 10 has these components communicating through the corpus store and the indexes, and that shared store is not implemented; with a persistent profile they would communicate that way, and the composition root would not change."
- The served configuration is not the best-ranking arm: its nDCG@10 is 0.8663, below dense-only (0.9493). The proposed retrieval metrics describe what is served; changing what is served is a decision to confirm on the held-out split before promotion, not after.
- The evaluation used the development split only; no number here comes from the held-out split. Where a held-out run was published, it is linked from the limitations list rather than promoted.
- ANSWER-LATENCY-P95 is over its PRD 9.3 budget: 4755.820900000001 ms against 3000. It is proposed as measured, not as met. Whether the breach was accepted, by whom and why is in docs/measurements/accepted-breaches.json.
- RETRIEVAL-STAGE-P95 is over its PRD 9.3 budget: 564.0732000000007 ms against 400. It is proposed as measured, not as met. Whether the breach was accepted, by whom and why is in docs/measurements/accepted-breaches.json.
- `content.problem` is the leading sentences of PRD section 1's first paragraph, cut at a sentence boundary to fit the schema's 600 characters rather than rewritten. Left out: "And it is unevenly permissioned — the security incident retro, the compensation policy, and the customer-specific runbook are each legitimately readable by different, overlapping sets of people." — decide whether the field needs it more than what precedes it.
- The stack lists only terms the portfolio vocabulary has. The models used — text-embedding-3-small and gpt-4.1-mini, over HTTP without an SDK (ADR 0006) — have no term in `technology.v1`; adding one is a reviewed taxonomy change, not part of this edit.

## What this verdict cannot decide

- **Whether to promote.** That is a person's reviewed edit to the portfolio manifest. This
  repository proposes; it never writes `content/projects/RAG-01.json`.
- **Whether the artefacts are true.** The checks establish that each artefact contains what
  PRD 12 names and agrees with the others. The commit, the corpus hash and the raw per-query and
  per-span files are what let somebody re-run the evaluation and the load run and compare.
- **External validation.** Promotion beyond `measured` needs an evidence item with
  `external: true`, and nothing in this project's scope produces one.

## Reproducing this

```bash
pnpm readiness         # decide, rewrite this file, write or remove the proposal
pnpm readiness:check   # fail if this file or the proposal disagrees with the artefacts
```
